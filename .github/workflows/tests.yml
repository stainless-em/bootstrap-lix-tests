name: Tests
on:
  push:
    branches: [ "main" ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: stainless-em/bootstrap-lix@1afe860efdf9e0e3356702acb0f5d48f9a0cb122
      - run: nix --version 2>&1 | grep "Lix, like Nix"
